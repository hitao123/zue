/**
 * Created by huahaitao on 18/1/24.
 *
 */
import { warn } from '../util/debug';
import { define, extend, proxy, mergeOptions } from '../util/lang';

export {
  warn,
  define,
  extend,
  proxy,
  mergeOptions
};