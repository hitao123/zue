/**
 * Created by huahaitao on 18/1/24.
 *
 */
import { warn } from './debug';
import { define, extend, proxy, mergeOptions } from './lang';
import { after, before, remove } from './dom';
export {
  warn,
  define,
  extend,
  proxy,
  mergeOptions,
  after,
  before,
  remove
};
